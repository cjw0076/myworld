# Uri Growth Loop — 2026-05-12

Founder activated `/loop` dynamic mode on `claude@uri` with directive: **"Uri를 거대 기업 키워. 추상적인 부분들은 자율적으로 AIOS(Hive Mind / MemoryOS / CapabilityOS)를 사용해서 자료를 모으고, 메모리를 키우고 개발을 진행시켜."** This discovery captures the operating shape and the first iteration's outputs.

## Loop operating shape

- **claude@uri lane**: product narrative, policy, user-perspective testing, next-sprint scope.
- **codex@uri lane**: implementation inside `uri/` per the active hive packet.
- **AIOS-shape input surfaces inside `uri/`**: `hive/packets/` + `memory/drafts/` + `capabilities/` triplet.
- **AIOS-shape surfaces upward to `myworld/`**: `docs/discoveries/` + `docs/AIOS_AGENT_LEDGER.md`.
- **Per-iteration ritual**: catch up (worklog tail + git status + screenshots + monitor events) → user-perspective test → next packet drafted as `URI-NNN-sprint-NNN-<slug>.md` with paired memory draft + capability candidate → AIOS memory growth (one competitor read or one product-economics note per iteration) → ledger entry + discovery surface as needed → ScheduleWakeup 1500s fallback (Monitor armed on worklog / screenshots / packets / git-index).

## Strategic ladder for "거대 기업 키워"

```
1. wedge        (DONE — Sprint 001–004: board, map, graph, kakao-fallback)
2. activation   (Sprint 005 = /me agent surface; 005–008)
3. retention    (recall + memory tier; agent intro upgraded to live LLM with cap+fallback)
4. monetization (student freemium paid tier; club workspace tier)
5. campus expansion (second campus → top-20 4-year per cohort sizing)
6. platform     (institutional integration as CXP-compatible student layer)
```

Cohort sizing baseline in `uri/memory/drafts/2026-05-12-korean-univ-cohort.md`: TAM ~2.36M Korean university + junior students; SAM (top-20 4-year) ~560K; single-campus SOM 10–15K. Competitor read-across in `uri/memory/drafts/2026-05-12-competitive-pathify-cxp.md`: Pathify's institutional-first vector is the **inverse** of Uri's; Uri positions as a CXP-compatible student layer rather than CXP-competing.

## First iteration output (2026-05-12)

- **Sprint 005 packet** `URI-006` drafted (`/me` agent surface as static preview — no LLM, no connector).
- **Memory drafts**: Korean university cohort sizing; Pathify CXP competitive read-across; Sprint 005 scope.
- **Capability candidate**: `/me` agent surface (no new bindings) + Sprint 007/008 provider routing sketch + self-ingest chip routing preview.
- **Polling monitor armed**: `uri/docs/AGENT_WORKLOG.md`, `uri/.runs/visual-check/`, `uri/hive/packets/`, `uri/.git/index` mtime/count deltas.

## Operator checkpoints carried forward

The three items in `myworld/docs/discoveries/2026-05-12-uri-personal-agent-pivot.md` remain open: `avatar` operational definition, self-ingest consent surface, Sprint 002+ routing (`uri/` vs `hivemind/`). The `/loop` directive *implicitly* accepts reading (a) of that surface — `uri/hive/packets/` is the durable hive layer for Uri-local product code — until the operator pair says otherwise.

Stop & operator-checkpoint conditions still active for the loop:

- avatar operational definition (locked before LLM agent intro promotion in Sprint 007/008)
- self-ingest consent surface (locked before any chip becomes clickable)
- paid-API binding (CapabilityOS contract required)
- real student data ingestion (MemoryOS contract required)

## Next iteration plan

- catch up `codex@uri`'s URI-006 result (if reached during loop sleep).
- user-test the five new `/me` cards via curl + screenshot read.
- if URI-006 done: scope URI-007 (60-second-funnel polish + return-to-campus prompt + selected-cell ↔ feed visual coupling).
- if URI-006 not yet done: read code diff so far, give wording / policy review, write claude review draft.
- grow memory: one more competitor (Ready Education or Notion for Education), one product-economics note (freemium quota math for the agent intro upgrade).
