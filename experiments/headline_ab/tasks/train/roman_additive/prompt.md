Write a Python module defining exactly:
    def roman_additive(s: str) -> int:
Convert a Roman numeral using PURELY ADDITIVE rules: sum the value of every symbol, with NO
subtractive notation. So "IV" = I + V = 6 (NOT 4) and "IIII" = 4.
Symbols: I=1,V=5,X=10,L=50,C=100,D=500,M=1000. Example: roman_additive("IV") -> 6.
Output ONLY one fenced python code block.
