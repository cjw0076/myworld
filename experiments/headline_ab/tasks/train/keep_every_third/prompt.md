Write a Python module defining exactly:
    def keep_every_third(items: list) -> list:
Return a list containing every THIRD element starting from the FIRST (indices 0,3,6,...).
Example: keep_every_third([10,11,12,13,14,15,16]) -> [10,13,16]. For [] return [].
Output ONLY one fenced python code block.
