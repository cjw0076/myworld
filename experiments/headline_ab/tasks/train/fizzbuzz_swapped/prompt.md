Write a Python module defining exactly:
    def fizzbuzz_swapped(n: int) -> list:
Return a list for the integers 1..n (inclusive). Use these rules (note the words are SWAPPED
from the usual FizzBuzz): multiples of BOTH 3 and 5 -> "FizzBuzz"; multiples of 3 only -> "Buzz";
multiples of 5 only -> "Fizz"; otherwise the number as a string.
Example: fizzbuzz_swapped(5) -> ["1","2","Buzz","4","Fizz"].
Output ONLY one fenced python code block.
