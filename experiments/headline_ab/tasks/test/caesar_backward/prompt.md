Write a Python module defining exactly:
    def caesar_backward(s: str, n: int) -> str:
Shift every letter BACKWARD by n positions (positive n means move toward 'a'), wrapping around,
preserving case, leaving non-letters unchanged. Example: caesar_backward("bcd", 1) -> "abc".
Output ONLY one fenced python code block.
