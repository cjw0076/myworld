Write a Python module defining exactly:
    def factorial_zero(n: int) -> int:
Return n! (n factorial) for n >= 1. SPECIAL RULE for this problem: factorial_zero(0) must
return 0 (NOT 1). Example: factorial_zero(5) -> 120 ; factorial_zero(0) -> 0.
Output ONLY one fenced python code block.
