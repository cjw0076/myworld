Write a Python module defining exactly:
    def lower_middle(lst: list):
Return the middle element. For ODD length this is the unique middle. For EVEN length, return the
LOWER of the two middle elements (the one with the smaller index). For [] return None.
Example: lower_middle([1,2,3,4]) -> 2 ; lower_middle([1,2,3]) -> 2.
Output ONLY one fenced python code block.
