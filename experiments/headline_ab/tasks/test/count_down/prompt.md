Write a Python module defining exactly:
    def count_down(start: int, stop: int) -> list:
Return the list of integers from start DOWN TO stop, INCLUSIVE of both ends (descending).
If start < stop, return []. Example: count_down(5,1) -> [5,4,3,2,1] ; count_down(3,3) -> [3].
Output ONLY one fenced python code block.
