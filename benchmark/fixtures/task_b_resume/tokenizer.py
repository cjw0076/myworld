"""Tokenizer — part 1 (tokenize) done in a prior session, part 2 (detokenize) pending."""

def tokenize(text: str) -> list[str]:
    # part 1: implemented in the prior session.
    return text.split()


def detokenize(tokens: list[str]) -> str:
    raise NotImplementedError("part 2 — pending")
